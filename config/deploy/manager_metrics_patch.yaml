# This patch adds the metrics exporter sidecar and configures
# the manager to expose metrics over HTTPS
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 8443
          name: metrics
          protocol: TCP

